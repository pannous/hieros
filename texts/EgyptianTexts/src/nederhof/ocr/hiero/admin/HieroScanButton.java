package nederhof.ocr.hiero.admin;

import nederhof.ocr.admin.*;
import nederhof.res.*;
import nederhof.res.format.*;
import nederhof.util.*;

public class HieroScanButton extends ScanGlyphButton {

	public HieroScanButton(String name, Prototype proto, int typeWidth, int typeHeight) {
		super(name, proto, typeWidth, typeHeight);
	}
}
